# Fork 贡献约定

本 Fork 不要求贡献者签署 CLA，不运行 CLA 检查、签名收集或合并后锁定讨论的自动化。

- 不保留 `CLA.md`、`.github/workflows/cla.yml` 或 `cla-signatures` 签名分支，不设置 CLA 必需检查。
- 上游同步时保留上述删除，禁止重新引入 CLA 工作流或要求上游作者在本 Fork 签署协议。
- 仓库现有 LICENSE 和上游版权声明保持有效；取消 Fork 的 CLA 流程不改变这些文件。
